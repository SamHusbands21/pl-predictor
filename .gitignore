# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Raw football-data CSVs are re-downloaded each run
data/raw/football_data/
# Processed features are rebuilt each run
data/processed/
# Understat xG JSONs ARE committed (understat.com blocks CI IPs)
# data/raw/understat/ — tracked

# Model artefacts — kept in repo so live pipeline doesn't need to retrain
# models/*.pkl
# models/*.joblib

# Secrets
.env
*.crt
*.key
*.pem

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# output/ is committed — recommendations.json is read by the website
